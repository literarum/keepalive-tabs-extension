# 📤 Инструкция: Загрузить проект на GitHub

## Шаг 1: Создать репозиторий на GitHub

1. Перейдите на [github.com](https://github.com)
2. Войдите в свой аккаунт (или создайте если нет)
3. Нажмите **"+"** в правом верхнем углу
4. Выберите **"New repository"**
5. Заполните форму:
    - **Repository name**: `keepalive-tabs-extension`
    - **Description**: `Chrome extension to keep pinned tabs alive`
    - **Public/Private**: Выберите Public (если хотите поделиться)
    - **Add .gitignore**: Пропустите (уже добавили)
    - **Add a license**: Выберите MIT
6. Нажмите **"Create repository"**

## Шаг 2: Инициализировать Git в папке проекта

Откройте PowerShell/Terminal в папке проекта:

```powershell
cd C:\Users\kurushin_gi\Desktop\keepalive-tabs-extension
```

Инициализируйте Git репозиторий:

```bash
git init
```

## Шаг 3: Добавить все файлы

```bash
git add .
```

## Шаг 4: Первый коммит

```bash
git commit -m "Initial commit: KeepAlive Tabs Extension"
```

## Шаг 5: Добавить удалённый репозиторий

Замените `YOUR_USERNAME` на ваше имя пользователя GitHub:

```bash
git branch -M main
git remote add origin https://github.com/YOUR_USERNAME/keepalive-tabs-extension.git
```

## Шаг 6: Загрузить на GitHub

```bash
git push -u origin main
```

При первой загрузке вас попросят аутентифицироваться:

- Используйте Personal Access Token (рекомендуется) или пароль GitHub
- Или используйте SSH ключ если он настроен

## Готово! ✅

Ваш проект теперь на GitHub:

- **URL**: `https://github.com/YOUR_USERNAME/keepalive-tabs-extension`
- **Клонирование**: `git clone https://github.com/YOUR_USERNAME/keepalive-tabs-extension.git`

## Дополнительные команды Git

### Проверить статус

```bash
git status
```

### Просмотреть логи коммитов

```bash
git log --oneline
```

### Создать новую ветку для разработки

```bash
git checkout -b feature/new-feature
git push -u origin feature/new-feature
```

### Обновить локальный репозиторий

```bash
git pull origin main
```

## Использование GitHub Desktop (альтернатива командной строке)

Если вам удобнее графический интерфейс:

1. Установите [GitHub Desktop](https://desktop.github.com/)
2. Откройте приложение и авторизуйтесь
3. File → Clone repository → URL
4. Вставьте ссылку вашего репозитория
5. Выберите локальную папку
6. Нажмите Clone

---

**Примечание:** Если у вас ещё нет Git, [скачайте отсюда](https://git-scm.com/download/win)
