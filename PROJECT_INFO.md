# KeepAlive Tabs Extension - Информация о проекте

## 📋 Статус проекта

- **Версия**: 1.0.0
- **Статус**: ✅ Функциональное расширение
- **Дата последнего обновления**: 4 февраля 2026 г.
- **Совместимость**: Chrome 88+, Edge 88+

## 🎯 Основные функции

### 1. Health-Check вкладок

- Периодическая проверка состояния закреплённых вкладок
- Настраиваемый интервал (минимум 60 секунд)
- Отправка скрытых XHR запросов для проверки доступности

### 2. Восстановление выгруженных вкладок

- Автоматическое восстановление вкладок со статусом "discarded"
- Опция отключения агрессивной перезагрузки

### 3. Автоматический вход

- Режимы:
    - **off** — отключено
    - **autofill** — автоматическое заполнение форм
    - **telegram** — автоклик по кнопке Telegram
- Поддержка кастомных селекторов для кликов

### 4. Правила сайтов

- Создание гибких правил по URL шаблонам
- Переопределение глобальных настроек на уровне сайта
- Управление селекторами readiness и click

### 5. Управление разрешениями

- Визуализация выданных host-разрешений
- Запрос разрешений прямо из UI
- Отображение требуемых и активных разрешений

## 📂 Структура файлов

```
keepalive-tabs-extension/
├── README.md                      # Основная документация
├── DEVELOPMENT.md                 # Руководство разработчика
├── GITHUB_SETUP.md               # Инструкция по GitHub
├── SLIDER_FIX_README.md          # Информация об исправлении слайдеров
├── manifest.json                  # Манифест расширения (Manifest V3)
├── package.json                   # Dependencies и scripts
├── .gitignore                    # Git ignore rules
│
├── src/                          # Исходный код расширения
│   ├── background/
│   │   └── service_worker.js     # (584 строк) Главная логика
│   ├── content/
│   │   ├── autologin.js          # (200+ строк) Автолог вход
│   │   └── healthcheck.js        # (150+ строк) Health-check
│   ├── options/
│   │   ├── options.html          # (272 строк) UI настроек с встроенными CSS
│   │   └── options.js            # (303 строк) Логика страницы настроек
│   ├── popup/
│   │   ├── index.html            # (343 строк) UI попапа
│   │   └── popup.js              # (200+ строк) Логика попапа
│   ├── common/                   # Общие модули
│   │   ├── defaults.js           # Настройки по умолчанию
│   │   ├── ids.js                # Генерация UUID
│   │   ├── messages.js           # Типы сообщений
│   │   ├── rules.js              # Логика правил (matching)
│   │   ├── siteRules.js          # Работа с правилами сайтов
│   │   ├── storage.js            # Chrome Storage API wrapper
│   │   ├── tabsRegistry.js       # Реестр вкладок
│   │   └── url.js                # Утилиты для URL
│   └── output.css                # Скомпилированные стили (Tailwind)
│
├── src_styles/
│   └── input.css                 # Исходные стили (Tailwind + @layer)
│
├── icons/
│   ├── icon16.png
│   ├── icon32.png
│   ├── icon48.png
│   └── icon128.png
│
└── [Тестовые файлы слайдеров - можно удалить]
    ├── slider-test.html
    ├── slider-alternative-test.html
    ├── direct-slider-test.html
    └── и другие...
```

## 🔑 Ключевые компоненты

### Service Worker (`service_worker.js`)

Главный контроллер расширения:

- **KeepAliveService** — основной класс
- `init()` — инициализация
- `runCycle()` — основной цикл проверок
- `_execHealthAndLogin()` — выполнение health-check и логина
- `_onMessage()` — обработка сообщений от UI

### Storage Schema

```javascript
{
    schemaVersion: 1,
    settings: {
        enabled: boolean,
        keepPinnedAlive: boolean,
        autoRestoreDiscarded: boolean,
        preventAutoDiscardable: boolean,
        healthIntervalSec: number (>=60),
        autoLoginMode: 'off' | 'autofill' | 'telegram',
        aggressiveReloadOnUnhealthy: boolean
    },
    siteRules: [
        {
            id: string (UUID),
            enabled: boolean,
            name: string,
            match: string (wildcard pattern),
            readinessSelector: string (CSS selector),
            loginMode: string,
            clickSelectors: string[] (CSS selectors)
        }
    ],
    pinnedEntries: {
        [urlKey]: {
            url: string,
            title: string,
            health: string,
            lastCheck: number (timestamp)
        }
    }
}
```

## 🎨 UI и Стили

### Tailwind CSS v4

- Используется в popup и options
- Кастомные стили через `@layer components`
- Собирается в `src/output.css`

### Встроенные стили

- Слайдеры (`input[type="range"]`) имеют встроенные стили в `<head>` options.html
- Это гарантирует работу без зависимостей от внешних CSS

## 🚀 Текущие возможности

✅ **Реализовано:**

- Health-check вкладок с настраиваемым интервалом
- Восстановление discarded вкладок
- Система правил сайтов с фильтрацией по URL
- Автоматический вход (autofill + telegram click)
- Управление host-разрешениями
- Диагностика в popup'е
- Встроенные слайдеры с анимацией
- Полная локализация на русский язык

🔄 **Можно улучшить:**

- Добавить больше режимов автолога (Facebook, Google и т.д.)
- Улучшить детекцию "готовности" страницы
- Добавить статистику использования
- Интеграция с CI/CD (GitHub Actions)
- Unit тесты
- E2E тесты для автолога

## 📊 Статистика кода

- **Всего строк JS**: ~2000+
- **Всего строк CSS**: ~1200+
- **Файлов HTML**: 3
- **Файлов модулей**: 8

## 🐛 Известные проблемы

Нет известных критических проблем.

### Возможные улучшения:

- Некоторые сайты могут требовать CAPTCHA (обработка не реализована)
- iframe-селекторы не поддерживаются для автолога (ограничение Chrome)

## 📝 Лицензия

MIT License - свободное использование и модификация

## 👨‍💻 Разработка

Для начала разработки см. [DEVELOPMENT.md](DEVELOPMENT.md)

Для загрузки на GitHub см. [GITHUB_SETUP.md](GITHUB_SETUP.md)

## 🔗 Полезные ссылки

- [Chrome Extension Docs](https://developer.chrome.com/docs/extensions/)
- [Manifest V3](https://developer.chrome.com/docs/extensions/mv3/)
- [Tailwind CSS](https://tailwindcss.com/)
- [Chrome Web Store Publishing](https://developer.chrome.com/docs/webstore/publish/)

---

**Created:** 4 февраля 2026 г.  
**Status:** Production Ready (v1.0.0)
