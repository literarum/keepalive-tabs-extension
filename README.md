# KeepAlive Tabs - Chrome Extension

🔌 Расширение для Chrome, которое поддерживает закреплённые вкладки "живыми" с помощью периодических health-check'ов, восстановления выгруженных вкладок и автоматического входа.

## ✨ Возможности

- **Health-check вкладок** — периодическая проверка состояния закреплённых вкладок
- **Восстановление Discarded** — автоматическая перезагрузка выгруженных вкладок
- **Автоматический вход** — поддержка автозаполнения и автоклика (telegram, кастомные селекторы)
- **Правила сайтов** — гибкие настройки по URL шаблонам
- **Управление разрешениями** — контроль host-разрешений для каждого сайта
- **Диагностика** — встроенная система отладки и мониторинга

## 📦 Установка

### Для разработки

1. Клонируйте репозиторий:

```bash
git clone https://github.com/yourusername/keepalive-tabs-extension.git
cd keepalive-tabs-extension
```

2. Установите зависимости:

```bash
npm install
```

3. Соберите CSS (Tailwind):

```bash
npm run build
```

4. Загрузите в Chrome:
    - Откройте `chrome://extensions/`
    - Включите "Режим разработчика"
    - Нажмите "Загрузить распакованное расширение"
    - Выберите папку проекта

## 🎨 Структура проекта

```
keepalive-tabs-extension/
├── src/
│   ├── background/           # Service Worker (основная логика)
│   │   └── service_worker.js
│   ├── content/             # Content Scripts (работают на страницах)
│   │   ├── autologin.js
│   │   └── healthcheck.js
│   ├── popup/               # Всплывающее окно расширения
│   │   ├── index.html
│   │   └── popup.js
│   ├── options/             # Страница настроек
│   │   ├── options.html
│   │   └── options.js
│   ├── common/              # Общие модули
│   │   ├── defaults.js
│   │   ├── ids.js
│   │   ├── messages.js
│   │   ├── rules.js
│   │   ├── siteRules.js
│   │   ├── storage.js
│   │   ├── tabsRegistry.js
│   │   └── url.js
│   ├── output.css           # Собранные стили (Tailwind)
├── src_styles/
│   └── input.css            # Исходные стили (Tailwind)
├── icons/                   # Иконки расширения
├── manifest.json            # Манифест расширения
├── package.json
└── README.md
```

## 🔧 Разработка

### Сборка CSS

```bash
npm run build
```

### Режим разработки (watch mode)

```bash
npm run watch
```

## 📝 Стили разработки

- **ES6+ JavaScript** с модулями
- **Tailwind CSS v4** для стилизации
- **Структурированный код** с классами и методами
- **Управление состоянием** через Chrome Storage API

## 🐛 Отладка

1. Откройте `chrome://extensions/`
2. Нажмите на "Детали" расширения
3. Перейдите в "Просмотр инспектора" (Service Worker)
4. Используйте консоль для логирования

## 📄 Лицензия

MIT

## 👨‍💻 Автор

Создано для автоматизации управления закреплёнными вкладками в Chrome.

---

**Текущая версия:** 1.0.0  
**Совместимость:** Chrome 88+, Edge 88+
